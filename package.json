{
  "name": "swag-studio",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run server.ts",
    "build": "bun build server.ts --outdir ./dist --target bun",
    "start": "bun run dist/server.js",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "bun": "^1.3.14",
    "typescript": "~5.8.2"
  }
}
